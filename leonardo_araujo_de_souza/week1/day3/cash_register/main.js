// <!-- # The Cash Register
//
// Write a function called cashRegister that takes a shopping cart object. The object contains item names and prices (itemName: itemPrice). The function should return the total price of the shopping cart.
// Example
//
// ``` Input
const cartForParty = {
  banana: "1.25",
  handkerchief: ".99",
  tshirt: "25.01",
  apple: "0.60",
  nalgene: "10.34",
  proteinShake: "22.36"
};

function cashRegister(cartForParty) {
  return banana + handkerchief + tshirt + apple + nalgene + proteinShake
}
console.log  (cartForParty);


// Output
const cart = {
total: "60.55",
};
function total(cart) {
  return  cart
}

console.log(cart);
