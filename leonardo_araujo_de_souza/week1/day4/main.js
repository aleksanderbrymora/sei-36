
// // // // My Transport Options




// document.querySelector('#Origin').classList.add('select destin')
// const select = seletor =>
// document.querySelectorAll(seletor).forEach(e => e.classList.add('select destin'))
// const disselect = seletor =>
// document.querySelectorAll(seletor).forEach(e => e.classList.remove('select destin'))


function Origim(line , station,stops, stopTotal) {
    return {
       line,
       station,
         stops,
        stopTotal,
    }
}
     console.log(trip('N','1st',1,3))




}
function Destin(line , station,stops, stopTotal) {
       line,
       station,
         stops,
        stopTotal,
    }
}
     console.log(trip('N','1st',1,3))
