// Create a new Javascript file and link to it with a script tag at the bottom.
// (In JS) Change the body tag's style so it has a font-family of "Arial, sans-serif".
// (In JS) Replace each of the spans (nickname, favorites, hometown) with your own information.
// Iterate through each li and change the class to "listitem". Add a style tag that sets a rule for "listitem" to make the color red.
// Create a new img element and set its src attribute to a picture of you. Append that element to the page.

$(body).css('font-family', 'Arial, sans serif')







// document.body.style.fontFamily = 'Arial, sans-serif';
// document.getElementById('nickname').innerHTML = 'Azi';
// document.getElementById('favorites').innerHTML = 'Blue';
// document.getElementById('hometown').innerHTML = 'Sydney';
// let listItems = document.querySelectorAll('li');
//
// for (let i = 0; i < listItems.length; i++) {
//   listItem = listItems[i];
//   listItem.className = 'listItem';
//
// }
// const image = document.createElement('img');
// image.src = 'http://www.fillmurray.com/80/150';
// document.body.appendChild(image);// attach to DOM
